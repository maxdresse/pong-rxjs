import { EffectFactory } from '../types';

/* export const keyboardEffects: EffectFactory = ({ addTeardownLogic }) => {
    window.addEventListener('keydown', ev => {
        if (ev.key === 'ArrowUp') {
            bodies[1].ApplyLinearImpulseToCenter({ x: 0, y: 120_0000 });
        } else if (ev.key === 'ArrowDown') {
            bodies[1].ApplyLinearImpulseToCenter({ x: 0, y: -120_0000 });
        }
    });  
}; */